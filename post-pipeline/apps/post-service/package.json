{
  "name": "@repo/post-service",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup --clean",
    "check-types": "tsc --noEmit",
    "dev": "tsup --watch --onSuccess \"pnpm start\"",
    "dev:debug": "tsup --watch --onSuccess \"node --enable-source-maps --inspect dist/index.js\"",
    "lint": "eslint .",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@repo/db": "workspace:^",
    "@repo/file-storage": "workspace:^",
    "dotenv": "^17.2.1",
    "express": "^4.21.2",
    "multer": "^2.0.2",
    "zod": "^4.0.14"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:^",
    "@repo/typescript-config": "workspace:^",
    "@types/express": "^5.0.3",
    "@types/multer": "^2.0.0",
    "tsup": "^8.5.0"
  }
}
